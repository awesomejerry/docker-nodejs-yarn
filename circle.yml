machine:
  services:
    - docker

test:
  override:
    - docker info

deployment:
  hub:
    branch: master
    commands:
      - docker login -u $DOCKER_USER -p $DOCKER_PASS
      - docker push awesomejerry/nodejs-yarn
